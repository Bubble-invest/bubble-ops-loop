# bubble-ops-console runtime deps (FastAPI cockpit).
# Install into the venv: venv/bin/pip install -r console/requirements.txt
fastapi==0.115.2
starlette==0.39.2
uvicorn==0.47.0
Jinja2==3.1.6
MarkupSafe==3.0.3
python-multipart==0.0.29
PyYAML==6.0.3
httpx==0.28.1
# whiteboard.md → sanitized HTML (markdown_render): the dept whiteboard is an
# agent-authored artifact rendered as markdown, sanitized to a strict allowlist.
Markdown==3.10.2
nh3==0.3.5
# jsonschema is required by the dry-run simulator (activate-dept path).
# ModuleNotFoundError was masking 409 responses during activation tests.
jsonschema>=4.0.0
# Ed25519 signing of inbox/decisions/*.yaml gate decisions (board #1476) —
# console/services/github_reader.py imports scripts.lib.decision_signing.
# Same package token-broker/requirements.txt already pins (>=42); pinned
# exact here to match console's own == convention.
cryptography==50.0.0
