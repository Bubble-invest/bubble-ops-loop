# Managed by bubble-vps-platform -- wire loop-layer@%i (legacy/manual GENERIC-mode floor)
# to the generic Telegram failure alerter.
# Card #1313 step 2 (code-review finding): the isolated per-dept templates
# (loop-layer1@..4@) were wired, but this legacy/manual-callable template
# — still documented as supported in scripts/loop-backup.sh's own header —
# was missed. Currently DORMANT in production (loop-backup.timer disabled,
# no live loop-layer@<slug> instances as of 2026-09-14) but must not stay a
# silent trap if it is ever exercised again.
[Unit]
OnFailure=cron-failure-alert@%n.service
