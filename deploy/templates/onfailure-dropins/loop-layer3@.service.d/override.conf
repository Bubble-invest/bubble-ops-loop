# Managed by bubble-vps-platform -- wire loop-layer3@%i (FLOOR) to the generic Telegram failure alerter.
# Card #1313: the loop FLOOR (fleet safety net) had ZERO OnFailure wiring, so its
# failures/deferrals died silently in `systemctl --failed`. See README.md.
[Unit]
OnFailure=cron-failure-alert@%n.service
