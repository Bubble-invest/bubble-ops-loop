# Managed by bubble-vps-platform -- wire loop-backup.service (legacy/manual
# GENERIC-mode floor, fired by loop-backup.timer) to the generic Telegram
# failure alerter.
# Card #1313 step 2 (code-review finding): same gap as loop-layer@.service.d
# — this unit was missed from the original OnFailure rollout. Currently
# DORMANT in production (loop-backup.timer is disabled as of 2026-09-14) but
# must not stay a silent trap if it is ever re-enabled.
[Unit]
OnFailure=cron-failure-alert@%n.service
